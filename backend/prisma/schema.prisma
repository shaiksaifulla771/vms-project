generator client {
  provider        = "prisma-client"
  output          = "../generated/prisma"
  previewFeatures = ["partialIndexes"]
  moduleFormat    = "cjs"
}

datasource db {
  provider = "postgresql"
  schemas  = ["auth", "public"]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_log_entries {
  instance_id String?   @db.Uuid
  id          String    @id @db.Uuid
  payload     Json?     @db.Json
  created_at  DateTime? @db.Timestamptz(6)
  ip_address  String    @default("") @db.VarChar(64)

  @@index([instance_id], map: "audit_logs_instance_id_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model custom_oauth_providers {
  id                      String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  provider_type           String
  identifier              String    @unique
  name                    String
  client_id               String
  client_secret           String
  acceptable_client_ids   String[]  @default([])
  scopes                  String[]  @default([])
  pkce_enabled            Boolean   @default(true)
  attribute_mapping       Json      @default("{}")
  authorization_params    Json      @default("{}")
  enabled                 Boolean   @default(true)
  email_optional          Boolean   @default(false)
  issuer                  String?
  discovery_url           String?
  skip_nonce_check        Boolean   @default(false)
  cached_discovery        Json?
  discovery_cached_at     DateTime? @db.Timestamptz(6)
  authorization_url       String?
  token_url               String?
  userinfo_url            String?
  jwks_uri                String?
  created_at              DateTime  @default(now()) @db.Timestamptz(6)
  updated_at              DateTime  @default(now()) @db.Timestamptz(6)
  custom_claims_allowlist String[]  @default([])

  @@index([created_at])
  @@index([enabled])
  @@index([identifier])
  @@index([provider_type])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model flow_state {
  id                     String                 @id @db.Uuid
  user_id                String?                @db.Uuid
  auth_code              String?
  code_challenge_method  code_challenge_method?
  code_challenge         String?
  provider_type          String
  provider_access_token  String?
  provider_refresh_token String?
  created_at             DateTime?              @db.Timestamptz(6)
  updated_at             DateTime?              @db.Timestamptz(6)
  authentication_method  String
  auth_code_issued_at    DateTime?              @db.Timestamptz(6)
  invite_token           String?
  referrer               String?
  oauth_client_state_id  String?                @db.Uuid
  linking_target_id      String?                @db.Uuid
  email_optional         Boolean                @default(false)
  saml_relay_states      saml_relay_states[]

  @@index([created_at(sort: Desc)])
  @@index([auth_code], map: "idx_auth_code")
  @@index([user_id, authentication_method], map: "idx_user_id_auth_method")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model identities {
  provider_id     String
  user_id         String    @db.Uuid
  identity_data   Json
  provider        String
  last_sign_in_at DateTime? @db.Timestamptz(6)
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)
  email           String?   @default(dbgenerated("lower((identity_data ->> 'email'::text))"))
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  users           users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([provider_id, provider], map: "identities_provider_id_provider_unique")
  @@index([email])
  @@index([user_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model instances {
  id              String    @id @db.Uuid
  uuid            String?   @db.Uuid
  raw_base_config String?
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_amr_claims {
  session_id            String   @db.Uuid
  created_at            DateTime @db.Timestamptz(6)
  updated_at            DateTime @db.Timestamptz(6)
  authentication_method String
  id                    String   @id(map: "amr_id_pk") @db.Uuid
  sessions              sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([session_id, authentication_method], map: "mfa_amr_claims_session_id_authentication_method_pkey")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_challenges {
  id                     String      @id @db.Uuid
  factor_id              String      @db.Uuid
  created_at             DateTime    @db.Timestamptz(6)
  verified_at            DateTime?   @db.Timestamptz(6)
  ip_address             String      @db.Inet
  otp_code               String?
  web_authn_session_data Json?
  mfa_factors            mfa_factors @relation(fields: [factor_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mfa_challenges_auth_factor_id_fkey")

  @@index([created_at(sort: Desc)], map: "mfa_challenge_created_at_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_factors {
  id                           String                  @id @db.Uuid
  user_id                      String                  @db.Uuid
  friendly_name                String?
  factor_type                  factor_type
  status                       factor_status
  created_at                   DateTime                @db.Timestamptz(6)
  updated_at                   DateTime                @db.Timestamptz(6)
  secret                       String?
  phone                        String?
  last_challenged_at           DateTime?               @unique @db.Timestamptz(6)
  web_authn_credential         Json?
  web_authn_aaguid             String?                 @db.Uuid
  last_webauthn_challenge_data Json?
  mfa_challenges               mfa_challenges[]
  users                        users                   @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mfa_recovery_code_sets       mfa_recovery_code_sets?

  @@unique([friendly_name, user_id], map: "mfa_factors_user_friendly_name_unique", where: raw("(TRIM(BOTH FROM friendly_name) <> ''::text)"))
  @@unique([user_id, phone], map: "unique_phone_factor_per_user")
  @@index([user_id, created_at], map: "factor_id_created_at_idx")
  @@index([user_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model mfa_recovery_code_sets {
  id                        String               @id @db.Uuid
  user_id                   String               @unique @db.Uuid
  mfa_factor_id             String               @unique @db.Uuid
  failed_verification_count Int                  @default(0)
  verification_locked_until DateTime?            @db.Timestamptz(6)
  created_at                DateTime             @default(now()) @db.Timestamptz(6)
  updated_at                DateTime             @default(now()) @db.Timestamptz(6)
  mfa_factors               mfa_factors          @relation(fields: [mfa_factor_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                     users                @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mfa_recovery_codes        mfa_recovery_codes[]

  @@schema("auth")
}

model mfa_recovery_codes {
  id                       String                 @id @db.Uuid
  mfa_recovery_code_set_id String                 @db.Uuid
  code_hash                String
  consumed_at              DateTime?              @db.Timestamptz(6)
  created_at               DateTime               @default(now()) @db.Timestamptz(6)
  mfa_recovery_code_sets   mfa_recovery_code_sets @relation(fields: [mfa_recovery_code_set_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([mfa_recovery_code_set_id], map: "mfa_recovery_codes_set_id_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_authorizations {
  id                    String                     @id @db.Uuid
  authorization_id      String                     @unique
  client_id             String                     @db.Uuid
  user_id               String?                    @db.Uuid
  redirect_uri          String
  scope                 String
  state                 String?
  resource              String?
  code_challenge        String?
  code_challenge_method code_challenge_method?
  response_type         oauth_response_type        @default(code)
  status                oauth_authorization_status @default(pending)
  authorization_code    String?                    @unique
  created_at            DateTime                   @default(now()) @db.Timestamptz(6)
  expires_at            DateTime                   @default(dbgenerated("(now() + '00:03:00'::interval)")) @db.Timestamptz(6)
  approved_at           DateTime?                  @db.Timestamptz(6)
  nonce                 String?
  oauth_clients         oauth_clients              @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 users?                     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires_at], map: "oauth_auth_pending_exp_idx", where: raw("(status = 'pending'::auth.oauth_authorization_status)"))
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oauth_client_states {
  id            String   @id @db.Uuid
  provider_type String
  code_verifier String?
  created_at    DateTime @db.Timestamptz(6)

  @@index([created_at], map: "idx_oauth_client_states_created_at")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_clients {
  id                         String                  @id @db.Uuid
  client_secret_hash         String?
  registration_type          oauth_registration_type
  redirect_uris              String
  grant_types                String
  client_name                String?
  client_uri                 String?
  logo_uri                   String?
  created_at                 DateTime                @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                @default(now()) @db.Timestamptz(6)
  deleted_at                 DateTime?               @db.Timestamptz(6)
  client_type                oauth_client_type       @default(confidential)
  token_endpoint_auth_method String
  oauth_authorizations       oauth_authorizations[]
  oauth_consents             oauth_consents[]
  sessions                   sessions[]

  @@index([deleted_at])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_consents {
  id            String        @id @db.Uuid
  user_id       String        @db.Uuid
  client_id     String        @db.Uuid
  scopes        String
  granted_at    DateTime      @default(now()) @db.Timestamptz(6)
  revoked_at    DateTime?     @db.Timestamptz(6)
  oauth_clients oauth_clients @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users         users         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, client_id], map: "oauth_consents_user_client_unique")
  @@index([client_id], map: "oauth_consents_active_client_idx", where: raw("(revoked_at IS NULL)"))
  @@index([user_id, client_id], map: "oauth_consents_active_user_client_idx", where: raw("(revoked_at IS NULL)"))
  @@index([user_id, granted_at(sort: Desc)], map: "oauth_consents_user_order_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model one_time_tokens {
  id         String              @id @db.Uuid
  user_id    String              @db.Uuid
  token_type one_time_token_type
  token_hash String
  relates_to String
  created_at DateTime            @default(now()) @db.Timestamp(6)
  updated_at DateTime            @default(now()) @db.Timestamp(6)
  expires_at DateTime?           @db.Timestamptz(6)
  users      users               @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, token_type])
  @@index([relates_to], map: "one_time_tokens_relates_to_hash_idx", type: Hash)
  @@index([token_hash], map: "one_time_tokens_token_hash_hash_idx", type: Hash)
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model refresh_tokens {
  instance_id String?   @db.Uuid
  id          BigInt    @id @default(autoincrement())
  token       String?   @unique(map: "refresh_tokens_token_unique") @db.VarChar(255)
  user_id     String?   @db.VarChar(255)
  revoked     Boolean?
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  parent      String?   @db.VarChar(255)
  session_id  String?   @db.Uuid
  sessions    sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([instance_id])
  @@index([instance_id, user_id])
  @@index([parent])
  @@index([session_id, revoked])
  @@index([updated_at(sort: Desc)])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_providers {
  id                String        @id @db.Uuid
  sso_provider_id   String        @db.Uuid
  entity_id         String        @unique
  metadata_xml      String
  metadata_url      String?
  attribute_mapping Json?
  created_at        DateTime?     @db.Timestamptz(6)
  updated_at        DateTime?     @db.Timestamptz(6)
  name_id_format    String?
  sso_providers     sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_relay_states {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  request_id      String
  for_email       String?
  redirect_to     String?
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  flow_state_id   String?       @db.Uuid
  flow_state      flow_state?   @relation(fields: [flow_state_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at(sort: Desc)])
  @@index([for_email])
  @@index([sso_provider_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schema_migrations {
  version String @id @db.VarChar(255)

  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model scim_tokens {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  token_hash      String        @unique
  prefix          String
  created_at      DateTime      @default(now()) @db.Timestamptz(6)
  expires_at      DateTime?     @db.Timestamptz(6)
  revoked_at      DateTime?     @db.Timestamptz(6)
  last_used_at    DateTime?     @db.Timestamptz(6)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires_at])
  @@index([revoked_at])
  @@index([sso_provider_id])
  @@schema("auth")
}

model scim_users {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  user_id         String?       @db.Uuid
  resource        Json
  user_name       String        @default(dbgenerated("lower((resource ->> 'userName'::text))"))
  external_id     String?       @default(dbgenerated("(resource ->> 'externalId'::text)"))
  active          Boolean       @default(dbgenerated("COALESCE(((resource ->> 'active'::text))::boolean, true)"))
  created_at      DateTime      @default(now()) @db.Timestamptz(6)
  updated_at      DateTime      @default(now()) @db.Timestamptz(6)
  deleted_at      DateTime?     @db.Timestamptz(6)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users           users?        @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@unique([sso_provider_id, external_id], map: "scim_users_external_id_key", where: raw("((external_id IS NOT NULL) AND (deleted_at IS NULL))"))
  @@unique([sso_provider_id, user_name], map: "scim_users_user_name_key", where: raw("(deleted_at IS NULL)"))
  @@index([sso_provider_id, created_at, id], map: "scim_users_created_at_idx", where: raw("(deleted_at IS NULL)"))
  @@index([deleted_at])
  @@index([sso_provider_id, id], map: "scim_users_id_idx", where: raw("(deleted_at IS NULL)"))
  @@index([sso_provider_id])
  @@index([sso_provider_id, updated_at, id], map: "scim_users_updated_at_idx", where: raw("(deleted_at IS NULL)"))
  @@index([user_id])
  @@index([sso_provider_id, user_name, id], map: "scim_users_user_name_idx", where: raw("(deleted_at IS NULL)"))
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sessions {
  id                     String           @id @db.Uuid
  user_id                String           @db.Uuid
  created_at             DateTime?        @db.Timestamptz(6)
  updated_at             DateTime?        @db.Timestamptz(6)
  factor_id              String?          @db.Uuid
  aal                    aal_level?
  not_after              DateTime?        @db.Timestamptz(6)
  refreshed_at           DateTime?        @db.Timestamp(6)
  user_agent             String?
  ip                     String?          @db.Inet
  tag                    String?
  oauth_client_id        String?          @db.Uuid
  refresh_token_hmac_key String?
  refresh_token_counter  BigInt?
  scopes                 String?
  mfa_amr_claims         mfa_amr_claims[]
  refresh_tokens         refresh_tokens[]
  oauth_clients          oauth_clients?   @relation(fields: [oauth_client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  users            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([not_after(sort: Desc)])
  @@index([oauth_client_id])
  @@index([user_id])
  @@index([user_id, created_at], map: "user_id_created_at_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_domains {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  domain          String
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_providers {
  id                String              @id @db.Uuid
  resource_id       String?
  created_at        DateTime?           @db.Timestamptz(6)
  updated_at        DateTime?           @db.Timestamptz(6)
  disabled          Boolean?
  saml_providers    saml_providers[]
  saml_relay_states saml_relay_states[]
  scim_tokens       scim_tokens[]
  scim_users        scim_users[]
  sso_domains       sso_domains[]

  @@index([resource_id], map: "sso_providers_resource_id_pattern_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model users {
  instance_id                 String?                 @db.Uuid
  id                          String                  @id @db.Uuid
  aud                         String?                 @db.VarChar(255)
  role                        String?                 @db.VarChar(255)
  email                       String?                 @unique(map: "users_email_partial_key", where: raw("(is_sso_user = false)")) @db.VarChar(255)
  encrypted_password          String?                 @db.VarChar(255)
  email_confirmed_at          DateTime?               @db.Timestamptz(6)
  invited_at                  DateTime?               @db.Timestamptz(6)
  confirmation_token          String?                 @unique(map: "confirmation_token_idx", where: raw("((confirmation_token)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  confirmation_sent_at        DateTime?               @db.Timestamptz(6)
  recovery_token              String?                 @unique(map: "recovery_token_idx", where: raw("((recovery_token)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  recovery_sent_at            DateTime?               @db.Timestamptz(6)
  email_change_token_new      String?                 @unique(map: "email_change_token_new_idx", where: raw("((email_change_token_new)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  email_change                String?                 @db.VarChar(255)
  email_change_sent_at        DateTime?               @db.Timestamptz(6)
  last_sign_in_at             DateTime?               @db.Timestamptz(6)
  raw_app_meta_data           Json?
  raw_user_meta_data          Json?
  is_super_admin              Boolean?
  created_at                  DateTime?               @db.Timestamptz(6)
  updated_at                  DateTime?               @db.Timestamptz(6)
  phone                       String?                 @unique
  phone_confirmed_at          DateTime?               @db.Timestamptz(6)
  phone_change                String?                 @default("")
  phone_change_token          String?                 @default("") @db.VarChar(255)
  phone_change_sent_at        DateTime?               @db.Timestamptz(6)
  confirmed_at                DateTime?               @default(dbgenerated("LEAST(email_confirmed_at, phone_confirmed_at)")) @db.Timestamptz(6)
  email_change_token_current  String?                 @unique(map: "email_change_token_current_idx", where: raw("((email_change_token_current)::text !~ '^[0-9 ]*$'::text)")) @default("") @db.VarChar(255)
  email_change_confirm_status Int?                    @default(0) @db.SmallInt
  banned_until                DateTime?               @db.Timestamptz(6)
  reauthentication_token      String?                 @unique(map: "reauthentication_token_idx", where: raw("((reauthentication_token)::text !~ '^[0-9 ]*$'::text)")) @default("") @db.VarChar(255)
  reauthentication_sent_at    DateTime?               @db.Timestamptz(6)
  is_sso_user                 Boolean                 @default(false)
  deleted_at                  DateTime?               @db.Timestamptz(6)
  is_anonymous                Boolean                 @default(false)
  identities                  identities[]
  mfa_factors                 mfa_factors[]
  mfa_recovery_code_sets      mfa_recovery_code_sets?
  oauth_authorizations        oauth_authorizations[]
  oauth_consents              oauth_consents[]
  one_time_tokens             one_time_tokens[]
  scim_users                  scim_users[]
  sessions                    sessions[]
  webauthn_challenges         webauthn_challenges[]
  webauthn_credentials        webauthn_credentials[]
  user_profiles               user_profiles?

  @@index([created_at(sort: Desc)], map: "idx_users_created_at_desc")
  @@index([email], map: "idx_users_email")
  @@index([last_sign_in_at(sort: Desc)], map: "idx_users_last_sign_in_at_desc")
  @@index([instance_id])
  @@index([is_anonymous])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model webauthn_challenges {
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String?  @db.Uuid
  challenge_type String
  session_data   Json
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  expires_at     DateTime @db.Timestamptz(6)
  users          users?   @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires_at])
  @@index([user_id])
  @@schema("auth")
}

model webauthn_credentials {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id          String    @db.Uuid
  credential_id    Bytes     @unique
  public_key       Bytes
  attestation_type String    @default("")
  aaguid           String?   @db.Uuid
  sign_count       BigInt    @default(0)
  transports       Json      @default("[]")
  backup_eligible  Boolean   @default(false)
  backed_up        Boolean   @default(false)
  friendly_name    String    @default("")
  created_at       DateTime  @default(now()) @db.Timestamptz(6)
  updated_at       DateTime  @default(now()) @db.Timestamptz(6)
  last_used_at     DateTime? @db.Timestamptz(6)
  users            users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id])
  @@schema("auth")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_log {
  id            String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  entity_type   String         @db.VarChar(50)
  entity_id     String         @db.Uuid
  action        audit_action
  actor_id      String?        @db.Uuid
  before        Json?
  after         Json?
  created_at    DateTime       @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  user_profiles user_profiles? @relation(fields: [actor_id], references: [id], onUpdate: NoAction)

  @@index([actor_id], map: "idx_audit_actor")
  @@index([created_at(sort: Desc)], map: "idx_audit_created_at")
  @@index([entity_type, entity_id], map: "idx_audit_entity")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model batch_actual_input_lots {
  id                    String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  batch_actual_input_id String              @db.Uuid
  consumed_lot_id       String              @db.Uuid
  quantity              Decimal             @db.Decimal(18, 4)
  created_at            DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  batch_actual_inputs   batch_actual_inputs @relation(fields: [batch_actual_input_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  inventory_lots        inventory_lots      @relation(fields: [consumed_lot_id], references: [id], onUpdate: NoAction)

  @@unique([batch_actual_input_id, consumed_lot_id], map: "uq_batch_input_lot")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model batch_actual_inputs {
  id                      String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  batch_record_id         String                    @db.Uuid
  material_id             String                    @db.Uuid
  bom_percentage          Decimal                   @db.Decimal(6, 3)
  planned_input_qty       Decimal                   @db.Decimal(18, 4)
  actual_input_qty        Decimal                   @db.Decimal(18, 4)
  variance_pct            Decimal                   @db.Decimal(5, 2)
  variance_reason         String?
  created_at              DateTime                  @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at              DateTime                  @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  batch_actual_input_lots batch_actual_input_lots[]
  batch_records           batch_records             @relation(fields: [batch_record_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  materials               materials                 @relation(fields: [material_id], references: [id], onUpdate: NoAction)

  @@unique([batch_record_id, material_id], map: "uq_batch_input_per_material")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model batch_records {
  id                                                    String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  batch_number                                          String                @unique @db.VarChar(30)
  plan_id                                               String?               @db.Uuid
  product_id                                            String                @db.Uuid
  location_id                                           String                @db.Uuid
  warehouse_id                                          String                @db.Uuid
  planned_output_qty                                    Decimal               @db.Decimal(18, 4)
  actual_output_qty                                     Decimal?              @db.Decimal(18, 4)
  output_variance_qty                                   Decimal?              @db.Decimal(18, 4)
  output_variance_pct                                   Decimal?              @db.Decimal(5, 2)
  output_variance_reason                                String?
  mfg_date                                              DateTime              @db.Date
  expiry_date                                           DateTime              @db.Date
  status                                                batch_status          @default(SCHEDULED)
  executed_by                                           String                @db.VarChar(100)
  created_by                                            String                @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                            String?               @db.Uuid
  created_at                                            DateTime              @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                            DateTime              @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  batch_actual_inputs                                   batch_actual_inputs[]
  user_profiles_batch_records_created_byTouser_profiles user_profiles         @relation("batch_records_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  locations                                             locations             @relation(fields: [location_id], references: [id], onUpdate: NoAction)
  plans                                                 plans?                @relation(fields: [plan_id], references: [id], onUpdate: NoAction)
  products                                              products              @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  user_profiles_batch_records_updated_byTouser_profiles user_profiles?        @relation("batch_records_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)
  warehouses                                            warehouses            @relation(fields: [warehouse_id], references: [id], onUpdate: NoAction)

  @@index([plan_id], map: "idx_batch_records_plan")
  @@index([status], map: "idx_batch_records_status")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model bom_items {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  bom_id             String    @db.Uuid
  material_id        String    @db.Uuid
  formula_percentage Decimal   @db.Decimal(6, 3)
  standard_qty       Decimal   @db.Decimal(18, 4)
  uom                String    @default("kg") @db.VarChar(20)
  boms               boms      @relation(fields: [bom_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  materials          materials @relation(fields: [material_id], references: [id], onUpdate: NoAction)

  @@unique([bom_id, material_id], map: "uq_bom_material")
  @@index([material_id], map: "idx_bom_items_material")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model boms {
  id                                           String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  product_id                                   String              @unique(map: "uq_boms_one_active_per_product", where: raw("(is_active = true)")) @db.Uuid
  version                                      Int                 @default(1)
  is_active                                    Boolean             @default(true)
  created_by                                   String              @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                   String?             @db.Uuid
  created_at                                   DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                   DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  deleted_at                                   DateTime?           @db.Timestamptz(6)
  deleted_by                                   String?             @db.Uuid
  bom_items                                    bom_items[]
  user_profiles_boms_created_byTouser_profiles user_profiles       @relation("boms_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_boms_deleted_byTouser_profiles user_profiles?      @relation("boms_deleted_byTouser_profiles", fields: [deleted_by], references: [id], onUpdate: NoAction)
  products                                     products            @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  user_profiles_boms_updated_byTouser_profiles user_profiles?      @relation("boms_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)
  mrp_runs                                     mrp_runs[]
  plan_products                                plan_products[]
  production_orders                            production_orders[]

  @@unique([product_id, version], map: "uq_bom_product_version")
  @@index([deleted_at], map: "idx_boms_deleted_at", where: raw("(deleted_at IS NOT NULL)"))
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model inventory_lots {
  id                                                     String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  lot_number                                             String                    @db.VarChar(100)
  material_id                                            String?                   @db.Uuid
  product_id                                             String?                   @db.Uuid
  location_id                                            String                    @db.Uuid
  warehouse_id                                           String                    @db.Uuid
  mfg_date                                               DateTime                  @db.Date
  expiry_date                                            DateTime                  @db.Date
  quantity_on_hand                                       Decimal                   @default(0) @db.Decimal(18, 4)
  uom                                                    String                    @default("kg") @db.VarChar(20)
  created_by                                             String                    @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                             String?                   @db.Uuid
  created_at                                             DateTime                  @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                             DateTime                  @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  batch_actual_input_lots                                batch_actual_input_lots[]
  user_profiles_inventory_lots_created_byTouser_profiles user_profiles             @relation("inventory_lots_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  locations                                              locations                 @relation(fields: [location_id], references: [id], onUpdate: NoAction)
  materials                                              materials?                @relation(fields: [material_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  products                                               products?                 @relation(fields: [product_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  user_profiles_inventory_lots_updated_byTouser_profiles user_profiles?            @relation("inventory_lots_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)
  warehouses                                             warehouses                @relation(fields: [warehouse_id], references: [id], onUpdate: NoAction)
  inventory_transactions                                 inventory_transactions[]

  @@unique([lot_number, warehouse_id], map: "uq_lot_in_warehouse")
  @@index([material_id, location_id, warehouse_id, expiry_date], map: "idx_lots_fefo")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model inventory_transactions {
  id               String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  lot_id           String             @db.Uuid
  transaction_type inventory_txn_type
  quantity         Decimal            @db.Decimal(18, 4)
  balance_after    Decimal            @db.Decimal(18, 4)
  reference_id     String?            @unique(map: "uq_single_mfg_production_per_batch", where: raw("(transaction_type = 'MFG_PRODUCTION'::inventory_txn_type)")) @db.VarChar(100)
  reason_notes     String?
  executed_by      String             @db.Uuid
  created_at       DateTime           @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  user_profiles    user_profiles      @relation(fields: [executed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  inventory_lots   inventory_lots     @relation(fields: [lot_id], references: [id], onUpdate: NoAction)

  @@unique([reference_id, lot_id], map: "uq_single_consumption_per_batch_lot", where: raw("(transaction_type = 'MFG_CONSUMPTION'::inventory_txn_type)"))
  @@index([lot_id], map: "idx_inventory_txn_lot")
  @@index([reference_id], map: "idx_inventory_txn_reference")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model locations {
  id                                                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                                              String              @unique @db.VarChar(100)
  code                                              String              @unique @db.VarChar(20)
  address                                           String?
  created_by                                        String              @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                        String?             @db.Uuid
  created_at                                        DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                        DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  appointments                                      appointments[]
  batch_records                                     batch_records[]
  inventory_lots                                    inventory_lots[]
  user_profiles_locations_created_byTouser_profiles user_profiles       @relation("locations_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_locations_updated_byTouser_profiles user_profiles?      @relation("locations_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)
  plan_products                                     plan_products[]
  production_orders                                 production_orders[]
  visitors                                          visitors[]
  warehouses                                        warehouses[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model material_vendors {
  id                                                       String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  material_id                                              String                        @unique(map: "uq_material_vendors_one_preferred", where: raw("(is_preferred = true)")) @db.Uuid
  vendor_id                                                String                        @db.Uuid
  mpn_code                                                 String                        @db.VarChar(80)
  specifications                                           Json                          @default("{}")
  certifications                                           String[]                      @default([])
  is_hazardous                                             Boolean                       @default(false)
  purchase_approved                                        Boolean                       @default(true)
  is_preferred                                             Boolean                       @default(false)
  moq                                                      Decimal                       @default(1) @db.Decimal(18, 4)
  lead_time_days                                           Int                           @default(7)
  status                                                   master_data_status            @default(ACTIVE)
  created_by                                               String                        @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                               String?                       @db.Uuid
  created_at                                               DateTime                      @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                               DateTime                      @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  deleted_at                                               DateTime?                     @db.Timestamptz(6)
  deleted_by                                               String?                       @db.Uuid
  user_profiles_material_vendors_created_byTouser_profiles user_profiles                 @relation("material_vendors_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_material_vendors_deleted_byTouser_profiles user_profiles?                @relation("material_vendors_deleted_byTouser_profiles", fields: [deleted_by], references: [id], onUpdate: NoAction)
  materials                                                materials                     @relation(fields: [material_id], references: [id], onUpdate: NoAction)
  user_profiles_material_vendors_updated_byTouser_profiles user_profiles?                @relation("material_vendors_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)
  vendors                                                  vendors                       @relation(fields: [vendor_id], references: [id], onUpdate: NoAction)
  production_order_components                              production_order_components[]
  purchase_order_items                                     purchase_order_items[]
  vendor_prices                                            vendor_prices[]

  @@unique([material_id, vendor_id], map: "uq_material_vendor")
  @@unique([vendor_id, mpn_code], map: "uq_vendor_mpn_code")
  @@index([deleted_at], map: "idx_material_vendors_deleted_at", where: raw("(deleted_at IS NOT NULL)"))
  @@index([material_id], map: "idx_material_vendors_material")
  @@index([vendor_id], map: "idx_material_vendors_vendor")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model materials {
  id                                                                        String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code                                                                      String                        @unique @db.VarChar(50)
  name                                                                      String                        @db.VarChar(150)
  classification                                                            item_classification
  uom                                                                       String                        @default("kg") @db.VarChar(20)
  hsn_code                                                                  String?                       @db.VarChar(20)
  safety_stock                                                              Decimal                       @default(0) @db.Decimal(18, 4)
  reorder_point                                                             Decimal                       @default(0) @db.Decimal(18, 4)
  moq                                                                       Decimal                       @default(1) @db.Decimal(18, 4)
  lead_time_days                                                            Int                           @default(7)
  is_hazardous                                                              Boolean                       @default(false)
  status                                                                    master_data_status            @default(ACTIVE)
  created_by                                                                String                        @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                                                String?                       @db.Uuid
  created_at                                                                DateTime                      @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                                                DateTime                      @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  deleted_at                                                                DateTime?                     @db.Timestamptz(6)
  deleted_by                                                                String?                       @db.Uuid
  batch_actual_inputs                                                       batch_actual_inputs[]
  bom_items                                                                 bom_items[]
  inventory_lots                                                            inventory_lots[]
  material_vendors                                                          material_vendors?
  user_profiles_materials_created_byTouser_profiles                         user_profiles                 @relation("materials_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_materials_deleted_byTouser_profiles                         user_profiles?                @relation("materials_deleted_byTouser_profiles", fields: [deleted_by], references: [id], onUpdate: NoAction)
  user_profiles_materials_updated_byTouser_profiles                         user_profiles?                @relation("materials_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)
  mrp_runs                                                                  mrp_runs[]
  planning_requirements_planning_requirements_material_idTomaterials        planning_requirements[]       @relation("planning_requirements_material_idTomaterials")
  planning_requirements_planning_requirements_parent_material_idTomaterials planning_requirements[]       @relation("planning_requirements_parent_material_idTomaterials")
  production_order_components                                               production_order_components[]
  purchase_order_items                                                      purchase_order_items[]
  purchase_request_items                                                    purchase_request_items[]

  @@index([classification], map: "idx_materials_classification")
  @@index([deleted_at], map: "idx_materials_deleted_at", where: raw("(deleted_at IS NOT NULL)"))
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model plan_products {
  id                String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  plan_id           String        @db.Uuid
  product_id        String        @db.Uuid
  location_id       String        @db.Uuid
  demand_target_qty Decimal       @db.Decimal(18, 4)
  batch_size_output Decimal       @db.Decimal(18, 4)
  bom_id            String        @db.Uuid
  batches_required  Int
  created_by        String        @default(dbgenerated("auth.uid()")) @db.Uuid
  created_at        DateTime      @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  boms              boms          @relation(fields: [bom_id], references: [id], onUpdate: NoAction)
  user_profiles     user_profiles @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  locations         locations     @relation(fields: [location_id], references: [id], onUpdate: NoAction)
  plans             plans         @relation(fields: [plan_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  products          products      @relation(fields: [product_id], references: [id], onUpdate: NoAction)

  @@unique([plan_id, product_id, location_id], map: "uq_plan_product_location")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model plans {
  id                                                        String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  plan_number                                               String              @unique @db.VarChar(30)
  status                                                    plan_status         @default(ACTIVE)
  idempotency_key                                           String?             @unique @db.VarChar(80)
  created_by                                                String              @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                                String?             @db.Uuid
  created_at                                                DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                                DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  batch_records                                             batch_records[]
  plan_products                                             plan_products[]
  user_profiles_plans_created_byTouser_profiles             user_profiles       @relation("plans_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_plans_updated_byTouser_profiles             user_profiles?      @relation("plans_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)
  production_orders_production_orders_plan_idToplans        production_orders[] @relation("production_orders_plan_idToplans")
  production_orders_production_orders_source_plan_idToplans production_orders[] @relation("production_orders_source_plan_idToplans")

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model products {
  id                                               String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sku                                              String              @unique @db.VarChar(50)
  name                                             String              @unique @db.VarChar(150)
  uom                                              String              @default("units") @db.VarChar(20)
  pack_size                                        Decimal?            @db.Decimal(12, 4)
  status                                           master_data_status  @default(ACTIVE)
  created_by                                       String              @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                       String?             @db.Uuid
  created_at                                       DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                       DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  variance_tolerance_percent                       Decimal             @default(5.00) @db.Decimal(5, 2)
  deleted_at                                       DateTime?           @db.Timestamptz(6)
  deleted_by                                       String?             @db.Uuid
  batch_records                                    batch_records[]
  boms                                             boms?
  inventory_lots                                   inventory_lots[]
  plan_products                                    plan_products[]
  production_orders                                production_orders[]
  user_profiles_products_created_byTouser_profiles user_profiles       @relation("products_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_products_deleted_byTouser_profiles user_profiles?      @relation("products_deleted_byTouser_profiles", fields: [deleted_by], references: [id], onUpdate: NoAction)
  user_profiles_products_updated_byTouser_profiles user_profiles?      @relation("products_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)

  @@index([deleted_at], map: "idx_products_deleted_at", where: raw("(deleted_at IS NOT NULL)"))
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model purchase_order_items {
  id                      String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  po_id                   String                    @db.Uuid
  line_no                 Int
  material_id             String                    @db.Uuid
  material_vendor_id      String?                   @db.Uuid
  quantity_ordered        Decimal                   @db.Decimal(18, 4)
  quantity_received       Decimal                   @default(0) @db.Decimal(18, 4)
  unit_price              Decimal                   @db.Decimal(18, 4)
  tax_percent             Decimal                   @default(0) @db.Decimal(5, 2)
  line_total              Decimal?                  @default(dbgenerated("round(((quantity_ordered * unit_price) * ((1)::numeric + (tax_percent / (100)::numeric))), 2)")) @db.Decimal(18, 2)
  materials               materials                 @relation(fields: [material_id], references: [id], onUpdate: NoAction)
  material_vendors        material_vendors?         @relation(fields: [material_vendor_id], references: [id], onUpdate: NoAction)
  purchase_orders         purchase_orders           @relation(fields: [po_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  purchase_order_receipts purchase_order_receipts[]

  @@unique([po_id, line_no], map: "uq_po_line")
  @@index([material_id], map: "idx_po_items_material")
  @@index([material_vendor_id], map: "idx_po_items_material_vendor")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model purchase_order_receipts {
  id                   String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  po_id                String               @db.Uuid
  po_item_id           String               @db.Uuid
  receipt_number       String               @db.VarChar(30)
  received_qty         Decimal              @db.Decimal(18, 4)
  received_at          DateTime             @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  on_time              Boolean              @default(true)
  quality_ok           Boolean              @default(true)
  notes                String?
  created_by           String               @default(dbgenerated("auth.uid()")) @db.Uuid
  created_at           DateTime             @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  user_profiles        user_profiles        @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  purchase_orders      purchase_orders      @relation(fields: [po_id], references: [id], onUpdate: NoAction)
  purchase_order_items purchase_order_items @relation(fields: [po_item_id], references: [id], onUpdate: NoAction)

  @@unique([po_item_id, receipt_number], map: "uq_po_item_receipt_number")
  @@index([po_item_id], map: "idx_po_receipts_item")
  @@index([po_id], map: "idx_po_receipts_po")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model purchase_orders {
  id                                                      String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  po_number                                               String                    @unique @db.VarChar(30)
  pr_id                                                   String?                   @db.Uuid
  vendor_id                                               String                    @db.Uuid
  status                                                  po_status                 @default(DRAFT)
  currency                                                String                    @default("INR") @db.Char(3)
  subtotal                                                Decimal                   @default(0) @db.Decimal(18, 2)
  tax_total                                               Decimal                   @default(0) @db.Decimal(18, 2)
  grand_total                                             Decimal                   @default(0) @db.Decimal(18, 2)
  expected_delivery_date                                  DateTime?                 @db.Date
  issued_at                                               DateTime?                 @db.Timestamptz(6)
  issued_by                                               String?                   @db.Uuid
  closed_at                                               DateTime?                 @db.Timestamptz(6)
  closed_by                                               String?                   @db.Uuid
  idempotency_key                                         String?                   @unique @db.VarChar(80)
  created_by                                              String                    @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                              String?                   @db.Uuid
  created_at                                              DateTime                  @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                              DateTime                  @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  purchase_order_items                                    purchase_order_items[]
  purchase_order_receipts                                 purchase_order_receipts[]
  user_profiles_purchase_orders_closed_byTouser_profiles  user_profiles?            @relation("purchase_orders_closed_byTouser_profiles", fields: [closed_by], references: [id], onUpdate: NoAction)
  user_profiles_purchase_orders_created_byTouser_profiles user_profiles             @relation("purchase_orders_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_purchase_orders_issued_byTouser_profiles  user_profiles?            @relation("purchase_orders_issued_byTouser_profiles", fields: [issued_by], references: [id], onUpdate: NoAction)
  purchase_requests                                       purchase_requests?        @relation(fields: [pr_id], references: [id], onUpdate: NoAction)
  user_profiles_purchase_orders_updated_byTouser_profiles user_profiles?            @relation("purchase_orders_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)
  vendors                                                 vendors                   @relation(fields: [vendor_id], references: [id], onUpdate: NoAction)

  @@index([pr_id], map: "idx_purchase_orders_pr")
  @@index([status], map: "idx_purchase_orders_status")
  @@index([vendor_id], map: "idx_purchase_orders_vendor")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model purchase_request_items {
  id                  String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  pr_id               String            @db.Uuid
  line_no             Int
  material_id         String            @db.Uuid
  quantity            Decimal           @db.Decimal(18, 4)
  uom                 String            @default("kg") @db.VarChar(20)
  suggested_vendor_id String?           @db.Uuid
  notes               String?
  materials           materials         @relation(fields: [material_id], references: [id], onUpdate: NoAction)
  purchase_requests   purchase_requests @relation(fields: [pr_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  vendors             vendors?          @relation(fields: [suggested_vendor_id], references: [id], onUpdate: NoAction)

  @@unique([pr_id, line_no], map: "uq_pr_line")
  @@index([material_id], map: "idx_pr_items_material")
  @@index([suggested_vendor_id], map: "idx_pr_items_suggested_vendor")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model purchase_requests {
  id                                                          String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  pr_number                                                   String                   @unique @db.VarChar(30)
  title                                                       String                   @db.VarChar(200)
  status                                                      pr_status                @default(DRAFT)
  required_by                                                 DateTime                 @db.Date
  justification                                               String?
  submitted_at                                                DateTime?                @db.Timestamptz(6)
  submitted_by                                                String?                  @db.Uuid
  decided_at                                                  DateTime?                @db.Timestamptz(6)
  decided_by                                                  String?                  @db.Uuid
  decision_notes                                              String?
  created_by                                                  String                   @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                                  String?                  @db.Uuid
  created_at                                                  DateTime                 @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                                  DateTime                 @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  purchase_orders                                             purchase_orders[]
  purchase_request_items                                      purchase_request_items[]
  user_profiles_purchase_requests_created_byTouser_profiles   user_profiles            @relation("purchase_requests_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_purchase_requests_decided_byTouser_profiles   user_profiles?           @relation("purchase_requests_decided_byTouser_profiles", fields: [decided_by], references: [id], onUpdate: NoAction)
  user_profiles_purchase_requests_submitted_byTouser_profiles user_profiles?           @relation("purchase_requests_submitted_byTouser_profiles", fields: [submitted_by], references: [id], onUpdate: NoAction)
  user_profiles_purchase_requests_updated_byTouser_profiles   user_profiles?           @relation("purchase_requests_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)

  @@index([required_by], map: "idx_purchase_requests_required_by")
  @@index([status], map: "idx_purchase_requests_status")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_profiles {
  id                                                                String                       @id @db.Uuid
  full_name                                                         String                       @db.VarChar(150)
  email                                                             String                       @unique @db.VarChar(255)
  role                                                              user_role                    @default(viewer)
  created_at                                                        DateTime                     @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                                        DateTime                     @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  appointments_appointments_approved_byTouser_profiles              appointments[]               @relation("appointments_approved_byTouser_profiles")
  appointments_appointments_created_byTouser_profiles               appointments[]               @relation("appointments_created_byTouser_profiles")
  appointments_appointments_host_user_idTouser_profiles             appointments[]               @relation("appointments_host_user_idTouser_profiles")
  approval_decisions                                                approval_decisions[]
  approval_requests                                                 approval_requests[]
  approval_workflows_approval_workflows_created_byTouser_profiles   approval_workflows[]         @relation("approval_workflows_created_byTouser_profiles")
  approval_workflows_approval_workflows_updated_byTouser_profiles   approval_workflows[]         @relation("approval_workflows_updated_byTouser_profiles")
  audit_log                                                         audit_log[]
  batch_records_batch_records_created_byTouser_profiles             batch_records[]              @relation("batch_records_created_byTouser_profiles")
  batch_records_batch_records_updated_byTouser_profiles             batch_records[]              @relation("batch_records_updated_byTouser_profiles")
  boms_boms_created_byTouser_profiles                               boms[]                       @relation("boms_created_byTouser_profiles")
  boms_boms_deleted_byTouser_profiles                               boms[]                       @relation("boms_deleted_byTouser_profiles")
  boms_boms_updated_byTouser_profiles                               boms[]                       @relation("boms_updated_byTouser_profiles")
  email_log                                                         email_log[]
  email_templates_email_templates_created_byTouser_profiles         email_templates[]            @relation("email_templates_created_byTouser_profiles")
  email_templates_email_templates_updated_byTouser_profiles         email_templates[]            @relation("email_templates_updated_byTouser_profiles")
  inventory_lots_inventory_lots_created_byTouser_profiles           inventory_lots[]             @relation("inventory_lots_created_byTouser_profiles")
  inventory_lots_inventory_lots_updated_byTouser_profiles           inventory_lots[]             @relation("inventory_lots_updated_byTouser_profiles")
  inventory_transactions                                            inventory_transactions[]
  locations_locations_created_byTouser_profiles                     locations[]                  @relation("locations_created_byTouser_profiles")
  locations_locations_updated_byTouser_profiles                     locations[]                  @relation("locations_updated_byTouser_profiles")
  material_vendors_material_vendors_created_byTouser_profiles       material_vendors[]           @relation("material_vendors_created_byTouser_profiles")
  material_vendors_material_vendors_deleted_byTouser_profiles       material_vendors[]           @relation("material_vendors_deleted_byTouser_profiles")
  material_vendors_material_vendors_updated_byTouser_profiles       material_vendors[]           @relation("material_vendors_updated_byTouser_profiles")
  materials_materials_created_byTouser_profiles                     materials[]                  @relation("materials_created_byTouser_profiles")
  materials_materials_deleted_byTouser_profiles                     materials[]                  @relation("materials_deleted_byTouser_profiles")
  materials_materials_updated_byTouser_profiles                     materials[]                  @relation("materials_updated_byTouser_profiles")
  mrp_runs                                                          mrp_runs[]
  notifications_notifications_recipient_user_idTouser_profiles      notifications[]              @relation("notifications_recipient_user_idTouser_profiles")
  notifications_notifications_related_user_idTouser_profiles        notifications[]              @relation("notifications_related_user_idTouser_profiles")
  plan_products                                                     plan_products[]
  plans_plans_created_byTouser_profiles                             plans[]                      @relation("plans_created_byTouser_profiles")
  plans_plans_updated_byTouser_profiles                             plans[]                      @relation("plans_updated_byTouser_profiles")
  production_orders_production_orders_approved_byTouser_profiles    production_orders[]          @relation("production_orders_approved_byTouser_profiles")
  production_orders_production_orders_completed_byTouser_profiles   production_orders[]          @relation("production_orders_completed_byTouser_profiles")
  production_orders_production_orders_created_byTouser_profiles     production_orders[]          @relation("production_orders_created_byTouser_profiles")
  production_orders_production_orders_qc_approved_byTouser_profiles production_orders[]          @relation("production_orders_qc_approved_byTouser_profiles")
  production_orders_production_orders_started_byTouser_profiles     production_orders[]          @relation("production_orders_started_byTouser_profiles")
  production_orders_production_orders_updated_byTouser_profiles     production_orders[]          @relation("production_orders_updated_byTouser_profiles")
  products_products_created_byTouser_profiles                       products[]                   @relation("products_created_byTouser_profiles")
  products_products_deleted_byTouser_profiles                       products[]                   @relation("products_deleted_byTouser_profiles")
  products_products_updated_byTouser_profiles                       products[]                   @relation("products_updated_byTouser_profiles")
  purchase_order_receipts                                           purchase_order_receipts[]
  purchase_orders_purchase_orders_closed_byTouser_profiles          purchase_orders[]            @relation("purchase_orders_closed_byTouser_profiles")
  purchase_orders_purchase_orders_created_byTouser_profiles         purchase_orders[]            @relation("purchase_orders_created_byTouser_profiles")
  purchase_orders_purchase_orders_issued_byTouser_profiles          purchase_orders[]            @relation("purchase_orders_issued_byTouser_profiles")
  purchase_orders_purchase_orders_updated_byTouser_profiles         purchase_orders[]            @relation("purchase_orders_updated_byTouser_profiles")
  purchase_requests_purchase_requests_created_byTouser_profiles     purchase_requests[]          @relation("purchase_requests_created_byTouser_profiles")
  purchase_requests_purchase_requests_decided_byTouser_profiles     purchase_requests[]          @relation("purchase_requests_decided_byTouser_profiles")
  purchase_requests_purchase_requests_submitted_byTouser_profiles   purchase_requests[]          @relation("purchase_requests_submitted_byTouser_profiles")
  purchase_requests_purchase_requests_updated_byTouser_profiles     purchase_requests[]          @relation("purchase_requests_updated_byTouser_profiles")
  quality_records_quality_records_created_byTouser_profiles         quality_records[]            @relation("quality_records_created_byTouser_profiles")
  quality_records_quality_records_dispositioned_byTouser_profiles   quality_records[]            @relation("quality_records_dispositioned_byTouser_profiles")
  quality_records_quality_records_inspector_idTouser_profiles       quality_records[]            @relation("quality_records_inspector_idTouser_profiles")
  quality_records_quality_records_updated_byTouser_profiles         quality_records[]            @relation("quality_records_updated_byTouser_profiles")
  users                                                             users                        @relation(fields: [id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  vendor_contracts_vendor_contracts_created_byTouser_profiles       vendor_contracts[]           @relation("vendor_contracts_created_byTouser_profiles")
  vendor_contracts_vendor_contracts_updated_byTouser_profiles       vendor_contracts[]           @relation("vendor_contracts_updated_byTouser_profiles")
  vendor_masters_vendor_masters_created_byTouser_profiles           vendor_masters[]             @relation("vendor_masters_created_byTouser_profiles")
  vendor_masters_vendor_masters_deleted_byTouser_profiles           vendor_masters[]             @relation("vendor_masters_deleted_byTouser_profiles")
  vendor_masters_vendor_masters_updated_byTouser_profiles           vendor_masters[]             @relation("vendor_masters_updated_byTouser_profiles")
  vendor_performance_ratings                                        vendor_performance_ratings[]
  vendor_prices_vendor_prices_created_byTouser_profiles             vendor_prices[]              @relation("vendor_prices_created_byTouser_profiles")
  vendor_prices_vendor_prices_updated_byTouser_profiles             vendor_prices[]              @relation("vendor_prices_updated_byTouser_profiles")
  vendors_vendors_created_byTouser_profiles                         vendors[]                    @relation("vendors_created_byTouser_profiles")
  vendors_vendors_deleted_byTouser_profiles                         vendors[]                    @relation("vendors_deleted_byTouser_profiles")
  vendors_vendors_updated_byTouser_profiles                         vendors[]                    @relation("vendors_updated_byTouser_profiles")
  visitors_visitors_created_byTouser_profiles                       visitors[]                   @relation("visitors_created_byTouser_profiles")
  visitors_visitors_host_employee_idTouser_profiles                 visitors[]                   @relation("visitors_host_employee_idTouser_profiles")
  warehouses_warehouses_created_byTouser_profiles                   warehouses[]                 @relation("warehouses_created_byTouser_profiles")
  warehouses_warehouses_updated_byTouser_profiles                   warehouses[]                 @relation("warehouses_updated_byTouser_profiles")
  workflows                                                         workflows[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This table contains exclusion constraints and requires additional setup for migrations. Visit https://pris.ly/d/exclusion-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model vendor_prices {
  id                                                    String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  material_vendor_id                                    String           @db.Uuid
  currency                                              String           @default("INR") @db.Char(3)
  unit_price                                            Decimal          @db.Decimal(18, 4)
  min_order_qty                                         Decimal          @default(1) @db.Decimal(18, 4)
  valid_from                                            DateTime         @db.Date
  valid_to                                              DateTime?        @db.Date
  source                                                price_source     @default(QUOTE)
  notes                                                 String?
  created_by                                            String           @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                            String?          @db.Uuid
  created_at                                            DateTime         @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                            DateTime         @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  user_profiles_vendor_prices_created_byTouser_profiles user_profiles    @relation("vendor_prices_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  material_vendors                                      material_vendors @relation(fields: [material_vendor_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  user_profiles_vendor_prices_updated_byTouser_profiles user_profiles?   @relation("vendor_prices_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)

  @@index([material_vendor_id], map: "idx_vendor_prices_mv")
  @@index([valid_from(sort: Desc)], map: "idx_vendor_prices_valid_from")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model vendors {
  id                                              String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code                                            String                       @unique @db.VarChar(50)
  name                                            String                       @unique @db.VarChar(150)
  legal_name                                      String?                      @db.VarChar(200)
  status                                          vendor_status                @default(DRAFT)
  contact_email                                   String?                      @db.VarChar(150)
  phone                                           String?                      @db.VarChar(20)
  gstin                                           String?                      @db.VarChar(15)
  pan                                             String?                      @db.VarChar(10)
  payment_terms_days                              Int                          @default(30)
  credit_limit                                    Decimal                      @default(0) @db.Decimal(18, 2)
  address_line1                                   String?                      @db.VarChar(200)
  address_line2                                   String?                      @db.VarChar(200)
  city                                            String?                      @db.VarChar(100)
  state                                           String?                      @db.VarChar(100)
  country                                         String?                      @db.VarChar(100)
  postal_code                                     String?                      @db.VarChar(20)
  notes                                           String?
  created_by                                      String                       @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                      String?                      @db.Uuid
  created_at                                      DateTime                     @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                      DateTime                     @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  deleted_at                                      DateTime?                    @db.Timestamptz(6)
  deleted_by                                      String?                      @db.Uuid
  material_vendors                                material_vendors[]
  purchase_orders                                 purchase_orders[]
  purchase_request_items                          purchase_request_items[]
  vendor_contracts                                vendor_contracts[]
  vendor_performance_ratings                      vendor_performance_ratings[]
  user_profiles_vendors_created_byTouser_profiles user_profiles                @relation("vendors_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_vendors_deleted_byTouser_profiles user_profiles?               @relation("vendors_deleted_byTouser_profiles", fields: [deleted_by], references: [id], onUpdate: NoAction)
  user_profiles_vendors_updated_byTouser_profiles user_profiles?               @relation("vendors_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)

  @@index([deleted_at], map: "idx_vendors_deleted_at", where: raw("(deleted_at IS NOT NULL)"))
  @@index([status], map: "idx_vendors_status")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model warehouses {
  id                                                                       String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  location_id                                                              String               @db.Uuid
  name                                                                     String               @db.VarChar(100)
  code                                                                     String               @db.VarChar(20)
  is_default                                                               Boolean              @default(false)
  created_by                                                               String               @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                                               String?              @db.Uuid
  created_at                                                               DateTime             @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                                               DateTime             @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  appointments                                                             appointments[]
  batch_records                                                            batch_records[]
  inventory_lots                                                           inventory_lots[]
  mrp_run_warehouses                                                       mrp_run_warehouses[]
  mrp_runs                                                                 mrp_runs[]
  production_orders_production_orders_destination_warehouse_idTowarehouses production_orders[]  @relation("production_orders_destination_warehouse_idTowarehouses")
  production_orders_production_orders_source_warehouse_idTowarehouses      production_orders[]  @relation("production_orders_source_warehouse_idTowarehouses")
  production_orders_production_orders_target_warehouse_idTowarehouses      production_orders[]  @relation("production_orders_target_warehouse_idTowarehouses")
  production_orders_production_orders_warehouse_idTowarehouses             production_orders[]  @relation("production_orders_warehouse_idTowarehouses")
  user_profiles_warehouses_created_byTouser_profiles                       user_profiles        @relation("warehouses_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  locations                                                                locations            @relation(fields: [location_id], references: [id], onUpdate: NoAction)
  user_profiles_warehouses_updated_byTouser_profiles                       user_profiles?       @relation("warehouses_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)

  @@unique([location_id, code], map: "uq_location_wh_code")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model appointments {
  id                                                     String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  appointment_number                                     String             @unique @db.VarChar(30)
  visitor_id                                             String             @db.Uuid
  host_user_id                                           String             @db.Uuid
  location_id                                            String             @db.Uuid
  warehouse_id                                           String?            @db.Uuid
  scheduled_start_time                                   DateTime           @db.Timestamptz(6)
  scheduled_end_time                                     DateTime           @db.Timestamptz(6)
  purpose                                                String
  status                                                 appointment_status @default(REQUESTED)
  approved_by                                            String?            @db.Uuid
  approval_notes                                         String             @default("")
  approval_time                                          DateTime?          @db.Timestamptz(6)
  created_by                                             String             @default(dbgenerated("auth.uid()")) @db.Uuid
  created_at                                             DateTime           @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                             DateTime           @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  user_profiles_appointments_approved_byTouser_profiles  user_profiles?     @relation("appointments_approved_byTouser_profiles", fields: [approved_by], references: [id], onUpdate: NoAction)
  user_profiles_appointments_created_byTouser_profiles   user_profiles      @relation("appointments_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_appointments_host_user_idTouser_profiles user_profiles      @relation("appointments_host_user_idTouser_profiles", fields: [host_user_id], references: [id], onUpdate: NoAction)
  locations                                              locations          @relation(fields: [location_id], references: [id], onUpdate: NoAction)
  visitors                                               visitors           @relation(fields: [visitor_id], references: [id], onUpdate: NoAction)
  warehouses                                             warehouses?        @relation(fields: [warehouse_id], references: [id], onUpdate: NoAction)

  @@index([host_user_id], map: "idx_appointments_host_user")
  @@index([location_id], map: "idx_appointments_location")
  @@index([scheduled_start_time], map: "idx_appointments_scheduled_start")
  @@index([status], map: "idx_appointments_status")
  @@index([visitor_id], map: "idx_appointments_visitor")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model approval_decisions {
  id                  String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  approval_request_id String                   @db.Uuid
  user_id             String                   @db.Uuid
  action              approval_decision_action
  step_order          Int
  reason              String?                  @default("")
  ip_address          String?                  @db.Inet
  decided_at          DateTime                 @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  approval_requests   approval_requests        @relation(fields: [approval_request_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  user_profiles       user_profiles            @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@index([approval_request_id], map: "idx_approval_decisions_request")
  @@index([user_id], map: "idx_approval_decisions_user")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model approval_requests {
  id                 String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workflow_id        String                  @db.Uuid
  entity_type        approval_entity_type
  entity_id          String                  @db.Uuid
  current_step       Int                     @default(1)
  status             approval_request_status @default(PENDING)
  requested_by       String                  @db.Uuid
  created_at         DateTime                @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at         DateTime                @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  approval_decisions approval_decisions[]
  user_profiles      user_profiles           @relation(fields: [requested_by], references: [id], onUpdate: NoAction)
  approval_workflows approval_workflows      @relation(fields: [workflow_id], references: [id], onUpdate: NoAction)

  @@index([entity_type, entity_id], map: "idx_approval_requests_entity")
  @@index([status], map: "idx_approval_requests_status")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model approval_steps {
  id                 String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workflow_id        String             @db.Uuid
  step_order         Int
  required_role      String             @db.VarChar(50)
  min_approvers      Int                @default(1)
  threshold_field    String?            @db.VarChar(100)
  threshold_value    Decimal?           @db.Decimal(18, 4)
  description        String?            @default("")
  approval_workflows approval_workflows @relation(fields: [workflow_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([workflow_id, step_order], map: "uq_approval_steps_order")
  @@index([workflow_id], map: "idx_approval_steps_workflow")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model approval_workflows {
  id                                                         String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  entity_type                                                approval_entity_type @unique
  is_active                                                  Boolean              @default(true)
  created_by                                                 String               @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                                 String?              @db.Uuid
  created_at                                                 DateTime             @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                                 DateTime             @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  approval_requests                                          approval_requests[]
  approval_steps                                             approval_steps[]
  user_profiles_approval_workflows_created_byTouser_profiles user_profiles        @relation("approval_workflows_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_approval_workflows_updated_byTouser_profiles user_profiles?       @relation("approval_workflows_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model email_log {
  id              String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  event_id        String?          @db.VarChar(100)
  event_type      String?          @db.VarChar(50)
  recipient       String           @db.VarChar(255)
  subject         String           @db.VarChar(255)
  template_code   String?          @db.VarChar(50)
  user_id         String?          @db.Uuid
  status          email_log_status
  sent_at         DateTime         @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  delivered_at    DateTime?        @db.Timestamptz(6)
  message_id      String?          @db.VarChar(150)
  attempts        Int              @default(0)
  last_error      String?
  error           String?
  metadata        Json?
  created_at      DateTime         @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at      DateTime         @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  email_templates email_templates? @relation(fields: [template_code], references: [template_code], onUpdate: NoAction)
  user_profiles   user_profiles?   @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@index([event_id], map: "idx_email_log_event_id", where: raw("(event_id IS NOT NULL)"))
  @@index([event_type], map: "idx_email_log_event_type")
  @@index([recipient], map: "idx_email_log_recipient")
  @@index([user_id], map: "idx_email_log_user")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model email_queue {
  id              String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  event_id        String?            @db.VarChar(100)
  event_type      String?            @db.VarChar(50)
  recipient       String             @db.VarChar(255)
  cc              String[]           @default([])
  bcc             String[]           @default([])
  subject         String             @db.VarChar(255)
  html_body       String
  text_body       String             @default("")
  template_code   String?            @db.VarChar(50)
  template_data   Json?
  status          email_queue_status @default(QUEUED)
  attempts        Int                @default(0)
  max_attempts    Int                @default(3)
  error_log       String[]           @default([])
  delivery_status String?            @db.VarChar(50)
  next_retry_at   DateTime?          @db.Timestamptz(6)
  last_attempt_at DateTime?          @db.Timestamptz(6)
  scheduled_for   DateTime           @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  sent_at         DateTime?          @db.Timestamptz(6)
  created_at      DateTime           @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at      DateTime           @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  email_templates email_templates?   @relation(fields: [template_code], references: [template_code], onUpdate: NoAction)

  @@index([event_id], map: "idx_email_queue_event_id", where: raw("(event_id IS NOT NULL)"))
  @@index([recipient], map: "idx_email_queue_recipient")
  @@index([scheduled_for], map: "idx_email_queue_scheduled_for")
  @@index([status], map: "idx_email_queue_status")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model email_templates {
  id                                                      String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  template_code                                           String                  @unique @db.VarChar(50)
  name                                                    String                  @db.VarChar(150)
  subject                                                 String                  @db.VarChar(255)
  html_body                                               String
  text_body                                               String                  @default("")
  variables                                               String[]                @default([])
  category                                                email_template_category @default(VISITOR)
  is_active                                               Boolean                 @default(true)
  created_by                                              String                  @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                              String?                 @db.Uuid
  created_at                                              DateTime                @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                              DateTime                @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  email_log                                               email_log[]
  email_queue                                             email_queue[]
  user_profiles_email_templates_created_byTouser_profiles user_profiles           @relation("email_templates_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_email_templates_updated_byTouser_profiles user_profiles?          @relation("email_templates_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)

  @@index([category], map: "idx_email_templates_category")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mrp_run_warehouses {
  mrp_run_id   String     @db.Uuid
  warehouse_id String     @db.Uuid
  mrp_runs     mrp_runs   @relation(fields: [mrp_run_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  warehouses   warehouses @relation(fields: [warehouse_id], references: [id], onUpdate: NoAction)

  @@id([mrp_run_id, warehouse_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mrp_runs {
  id                                  String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  run_number                          String                  @unique @db.VarChar(30)
  material_id                         String                  @db.Uuid
  bom_id                              String                  @db.Uuid
  bom_version                         Int                     @default(1)
  warehouse_id                        String                  @db.Uuid
  target_qty                          Decimal                 @db.Decimal(18, 4)
  required_date                       DateTime                @db.Date
  horizon_days                        Int                     @default(30)
  parameters                          Json                    @default("{}")
  algorithm_version                   String                  @default("MRP-2.1") @db.VarChar(30)
  planning_rule_version               String                  @default("RULESET-1.4") @db.VarChar(30)
  idempotency_key                     String?                 @unique(map: "uq_mrp_runs_idempotency_key", where: raw("(idempotency_key IS NOT NULL)")) @db.VarChar(80)
  input_hash                          String?                 @db.VarChar(128)
  input_snapshot                      Json?
  candidate_proposal                  Json?
  status                              mrp_run_status          @default(COMPLETED)
  summary_total_components            Int?
  summary_total_shortages             Int?
  summary_has_shortage                Boolean?
  summary_total_production_plans      Int?
  summary_total_purchase_requirements Int?
  summary_ai_explanation              String?
  exceptions                          Json                    @default("[]")
  executed_by                         String?                 @db.Uuid
  created_at                          DateTime                @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  mrp_run_warehouses                  mrp_run_warehouses[]
  boms                                boms                    @relation(fields: [bom_id], references: [id], onUpdate: NoAction)
  user_profiles                       user_profiles?          @relation(fields: [executed_by], references: [id], onUpdate: NoAction)
  materials                           materials               @relation(fields: [material_id], references: [id], onUpdate: NoAction)
  warehouses                          warehouses              @relation(fields: [warehouse_id], references: [id], onUpdate: NoAction)
  planning_requirements               planning_requirements[]
  production_orders                   production_orders[]

  @@index([created_at(sort: Desc)], map: "idx_mrp_runs_created_at")
  @@index([material_id, created_at(sort: Desc)], map: "idx_mrp_runs_material")
  @@index([warehouse_id], map: "idx_mrp_runs_warehouse")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model notifications {
  id                                                           String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  recipient_user_id                                            String?                      @db.Uuid
  recipient_role                                               notification_recipient_role?
  type                                                         notification_type
  title                                                        String                       @default("System Notification") @db.VarChar(150)
  message                                                      String
  related_user_id                                              String?                      @db.Uuid
  metadata                                                     Json                         @default("{}")
  severity                                                     notification_severity        @default(INFO)
  is_read                                                      Boolean                      @default(false)
  created_at                                                   DateTime                     @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  user_profiles_notifications_recipient_user_idTouser_profiles user_profiles?               @relation("notifications_recipient_user_idTouser_profiles", fields: [recipient_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  user_profiles_notifications_related_user_idTouser_profiles   user_profiles?               @relation("notifications_related_user_idTouser_profiles", fields: [related_user_id], references: [id], onUpdate: NoAction)

  @@index([recipient_role, is_read, created_at(sort: Desc)], map: "idx_notifications_recipient_role", where: raw("(recipient_role IS NOT NULL)"))
  @@index([recipient_user_id, is_read, created_at(sort: Desc)], map: "idx_notifications_recipient_user", where: raw("(recipient_user_id IS NOT NULL)"))
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model planning_requirements {
  id                                                            String                               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mrp_run_id                                                    String                               @db.Uuid
  source_key                                                    String                               @unique @db.VarChar(150)
  material_id                                                   String                               @db.Uuid
  material_code                                                 String?                              @db.VarChar(50)
  material_name                                                 String?                              @db.VarChar(150)
  unit                                                          String?                              @db.VarChar(20)
  required_qty                                                  Decimal                              @db.Decimal(18, 4)
  available_qty                                                 Decimal                              @default(0) @db.Decimal(18, 4)
  reserved_qty                                                  Decimal                              @default(0) @db.Decimal(18, 4)
  on_order_qty                                                  Decimal                              @default(0) @db.Decimal(18, 4)
  net_qty                                                       Decimal                              @default(0) @db.Decimal(18, 4)
  shortage_qty                                                  Decimal                              @default(0) @db.Decimal(18, 4)
  suggested_lead_time_days                                      Int                                  @default(7)
  action                                                        planning_requirement_action          @default(SUFFICIENT)
  shortage_reason                                               planning_requirement_shortage_reason @default(SUFFICIENT)
  level                                                         Int                                  @default(1)
  parent_material_id                                            String?                              @db.Uuid
  parent_material_code                                          String?                              @db.VarChar(50)
  requirement_date                                              DateTime?                            @db.Date
  release_date                                                  DateTime?                            @db.Date
  due_date                                                      DateTime?                            @db.Date
  trace                                                         Json                                 @default("{}")
  status                                                        planning_requirement_status          @default(PENDING)
  created_at                                                    DateTime                             @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  materials_planning_requirements_material_idTomaterials        materials                            @relation("planning_requirements_material_idTomaterials", fields: [material_id], references: [id], onUpdate: NoAction)
  mrp_runs                                                      mrp_runs                             @relation(fields: [mrp_run_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  materials_planning_requirements_parent_material_idTomaterials materials?                           @relation("planning_requirements_parent_material_idTomaterials", fields: [parent_material_id], references: [id], onUpdate: NoAction)

  @@index([material_id, requirement_date], map: "idx_planning_requirements_material_date")
  @@index([mrp_run_id], map: "idx_planning_requirements_mrp_run")
  @@index([parent_material_id], map: "idx_planning_requirements_parent_material")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model production_order_components {
  id                  String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  production_order_id String            @db.Uuid
  material_id         String?           @db.Uuid
  mpn_id              String?           @db.Uuid
  expected_quantity   Decimal           @db.Decimal(18, 4)
  actual_quantity     Decimal           @default(0) @db.Decimal(18, 4)
  consumed_quantity   Decimal           @default(0) @db.Decimal(18, 4)
  scrap_quantity      Decimal           @default(0) @db.Decimal(18, 4)
  loss_percent        Decimal           @default(0) @db.Decimal(5, 2)
  expected_cost       Decimal           @default(0) @db.Decimal(18, 2)
  actual_cost         Decimal           @default(0) @db.Decimal(18, 2)
  variance_quantity   Decimal           @default(0) @db.Decimal(18, 4)
  materials           materials?        @relation(fields: [material_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  material_vendors    material_vendors? @relation(fields: [mpn_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  production_orders   production_orders @relation(fields: [production_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([material_id], map: "idx_production_order_components_material")
  @@index([mpn_id], map: "idx_production_order_components_mpn")
  @@index([production_order_id], map: "idx_production_order_components_order")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model production_orders {
  id                                                                String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  prd_number                                                        String                        @unique @db.VarChar(30)
  plan_id                                                           String?                       @db.Uuid
  source_plan_id                                                    String?                       @db.Uuid
  source_plan_number                                                String?                       @db.VarChar(30)
  source_mrp_run_id                                                 String?                       @db.Uuid
  bom_id                                                            String                        @db.Uuid
  product_id                                                        String                        @db.Uuid
  location_id                                                       String?                       @db.Uuid
  warehouse_id                                                      String?                       @db.Uuid
  source_warehouse_id                                               String                        @db.Uuid
  destination_warehouse_id                                          String?                       @db.Uuid
  target_warehouse_id                                               String?                       @db.Uuid
  target_quantity                                                   Decimal                       @db.Decimal(18, 4)
  actual_quantity                                                   Decimal                       @default(0) @db.Decimal(18, 4)
  scrap_quantity                                                    Decimal                       @default(0) @db.Decimal(18, 4)
  waste_quantity                                                    Decimal                       @default(0) @db.Decimal(18, 4)
  yield_percent                                                     Decimal?                      @db.Decimal(6, 2)
  waste_reason                                                      String?
  variance_reason                                                   String?
  batch_number                                                      String?                       @db.VarChar(100)
  lot_number                                                        String?                       @db.VarChar(100)
  mfg_date                                                          DateTime?                     @db.Date
  expiry_date                                                       DateTime?                     @db.Date
  status                                                            production_order_status       @default(DRAFT)
  expected_cost                                                     Decimal                       @default(0) @db.Decimal(18, 2)
  actual_cost                                                       Decimal                       @default(0) @db.Decimal(18, 2)
  material_variance                                                 Decimal                       @default(0) @db.Decimal(18, 4)
  cost_variance                                                     Decimal                       @default(0) @db.Decimal(18, 2)
  version                                                           Int                           @default(1)
  created_by                                                        String                        @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                                        String?                       @db.Uuid
  approved_by                                                       String?                       @db.Uuid
  started_by                                                        String?                       @db.Uuid
  completed_by                                                      String?                       @db.Uuid
  qc_approved_by                                                    String?                       @db.Uuid
  ip_address                                                        String?                       @db.Inet
  user_agent                                                        String?
  created_at                                                        DateTime                      @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                                        DateTime                      @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  production_order_components                                       production_order_components[]
  user_profiles_production_orders_approved_byTouser_profiles        user_profiles?                @relation("production_orders_approved_byTouser_profiles", fields: [approved_by], references: [id], onUpdate: NoAction)
  boms                                                              boms                          @relation(fields: [bom_id], references: [id], onUpdate: NoAction)
  user_profiles_production_orders_completed_byTouser_profiles       user_profiles?                @relation("production_orders_completed_byTouser_profiles", fields: [completed_by], references: [id], onUpdate: NoAction)
  user_profiles_production_orders_created_byTouser_profiles         user_profiles                 @relation("production_orders_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  warehouses_production_orders_destination_warehouse_idTowarehouses warehouses?                   @relation("production_orders_destination_warehouse_idTowarehouses", fields: [destination_warehouse_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  locations                                                         locations?                    @relation(fields: [location_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  plans_production_orders_plan_idToplans                            plans?                        @relation("production_orders_plan_idToplans", fields: [plan_id], references: [id], onUpdate: NoAction)
  products                                                          products                      @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  user_profiles_production_orders_qc_approved_byTouser_profiles     user_profiles?                @relation("production_orders_qc_approved_byTouser_profiles", fields: [qc_approved_by], references: [id], onUpdate: NoAction)
  mrp_runs                                                          mrp_runs?                     @relation(fields: [source_mrp_run_id], references: [id], onUpdate: NoAction)
  plans_production_orders_source_plan_idToplans                     plans?                        @relation("production_orders_source_plan_idToplans", fields: [source_plan_id], references: [id], onUpdate: NoAction)
  warehouses_production_orders_source_warehouse_idTowarehouses      warehouses                    @relation("production_orders_source_warehouse_idTowarehouses", fields: [source_warehouse_id], references: [id], onUpdate: NoAction)
  user_profiles_production_orders_started_byTouser_profiles         user_profiles?                @relation("production_orders_started_byTouser_profiles", fields: [started_by], references: [id], onUpdate: NoAction)
  warehouses_production_orders_target_warehouse_idTowarehouses      warehouses?                   @relation("production_orders_target_warehouse_idTowarehouses", fields: [target_warehouse_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  user_profiles_production_orders_updated_byTouser_profiles         user_profiles?                @relation("production_orders_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)
  warehouses_production_orders_warehouse_idTowarehouses             warehouses?                   @relation("production_orders_warehouse_idTowarehouses", fields: [warehouse_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  quality_records                                                   quality_records?

  @@index([bom_id], map: "idx_production_orders_bom")
  @@index([created_at(sort: Desc)], map: "idx_production_orders_created_at")
  @@index([destination_warehouse_id], map: "idx_production_orders_destination_warehouse")
  @@index([plan_id], map: "idx_production_orders_plan")
  @@index([product_id], map: "idx_production_orders_product")
  @@index([source_warehouse_id], map: "idx_production_orders_source_warehouse")
  @@index([status], map: "idx_production_orders_status")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model quality_records {
  id                                                            String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  production_order_id                                           String              @unique @db.Uuid
  checkpoint                                                    quality_checkpoint  @default(FINISHED_GOODS)
  disposition                                                   quality_disposition @default(PENDING)
  samples                                                       Json                @default("{}")
  specifications                                                Json                @default("{}")
  measurements                                                  Json                @default("{}")
  defects                                                       Json                @default("[]")
  attachments                                                   Json                @default("[]")
  notes                                                         String?
  inspector_id                                                  String              @default(dbgenerated("auth.uid()")) @db.Uuid
  dispositioned_at                                              DateTime?           @db.Timestamptz(6)
  dispositioned_by                                              String?             @db.Uuid
  created_by                                                    String              @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                                    String?             @db.Uuid
  created_at                                                    DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                                    DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  user_profiles_quality_records_created_byTouser_profiles       user_profiles       @relation("quality_records_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_quality_records_dispositioned_byTouser_profiles user_profiles?      @relation("quality_records_dispositioned_byTouser_profiles", fields: [dispositioned_by], references: [id], onUpdate: NoAction)
  user_profiles_quality_records_inspector_idTouser_profiles     user_profiles       @relation("quality_records_inspector_idTouser_profiles", fields: [inspector_id], references: [id], onUpdate: NoAction)
  production_orders                                             production_orders   @relation(fields: [production_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  user_profiles_quality_records_updated_byTouser_profiles       user_profiles?      @relation("quality_records_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)

  @@index([checkpoint], map: "idx_quality_records_checkpoint")
  @@index([disposition], map: "idx_quality_records_disposition")
  @@index([inspector_id], map: "idx_quality_records_inspector")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model vendor_contracts {
  id                                                       String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  vendor_id                                                String          @db.Uuid
  title                                                    String          @db.VarChar(200)
  start_date                                               DateTime        @db.Date
  end_date                                                 DateTime        @db.Date
  status                                                   contract_status @default(PENDING)
  value                                                    Decimal         @db.Decimal(18, 2)
  document_url                                             String          @default("")
  created_by                                               String          @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                               String?         @db.Uuid
  created_at                                               DateTime        @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                               DateTime        @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  user_profiles_vendor_contracts_created_byTouser_profiles user_profiles   @relation("vendor_contracts_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_vendor_contracts_updated_byTouser_profiles user_profiles?  @relation("vendor_contracts_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)
  vendors                                                  vendors         @relation(fields: [vendor_id], references: [id], onUpdate: NoAction)

  @@index([end_date], map: "idx_vendor_contracts_end_date")
  @@index([status], map: "idx_vendor_contracts_status")
  @@index([vendor_id], map: "idx_vendor_contracts_vendor")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model vendor_master_contacts {
  id               String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  vendor_master_id String         @db.Uuid
  name             String         @default("") @db.VarChar(150)
  phone            String         @default("") @db.VarChar(20)
  contact_role     String         @default("Other") @db.VarChar(50)
  department       String         @default("Sourcing") @db.VarChar(50)
  email            String         @default("") @db.VarChar(150)
  vendor_masters   vendor_masters @relation(fields: [vendor_master_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([vendor_master_id], map: "idx_vendor_master_contacts_vendor_master")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model vendor_masters {
  id                                                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  vendor_id_code                                         String                   @unique(map: "uq_vendor_masters_vendor_id_active", where: raw("(deleted_at IS NULL)")) @db.VarChar(50)
  company_name                                           String                   @db.VarChar(200)
  tax_id                                                 String                   @unique(map: "uq_vendor_masters_tax_id_active", where: raw("(deleted_at IS NULL)")) @db.VarChar(50)
  contact_email                                          String                   @db.VarChar(150)
  department                                             String                   @default("") @db.VarChar(100)
  role_title                                             String                   @default("") @db.VarChar(100)
  status                                                 master_data_status       @default(ACTIVE)
  deleted_at                                             DateTime?                @db.Timestamptz(6)
  deleted_by                                             String?                  @db.Uuid
  created_by                                             String                   @default(dbgenerated("auth.uid()")) @db.Uuid
  updated_by                                             String?                  @db.Uuid
  created_at                                             DateTime                 @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                             DateTime                 @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  vendor_master_contacts                                 vendor_master_contacts[]
  user_profiles_vendor_masters_created_byTouser_profiles user_profiles            @relation("vendor_masters_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_vendor_masters_deleted_byTouser_profiles user_profiles?           @relation("vendor_masters_deleted_byTouser_profiles", fields: [deleted_by], references: [id], onUpdate: NoAction)
  user_profiles_vendor_masters_updated_byTouser_profiles user_profiles?           @relation("vendor_masters_updated_byTouser_profiles", fields: [updated_by], references: [id], onUpdate: NoAction)

  @@index([deleted_at], map: "idx_vendor_masters_deleted_at", where: raw("(deleted_at IS NOT NULL)"))
  @@index([status], map: "idx_vendor_masters_status")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model vendor_performance_ratings {
  id            String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  vendor_id     String        @db.Uuid
  rating        Int           @db.SmallInt
  feedback      String
  rated_by      String        @db.Uuid
  created_at    DateTime      @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  user_profiles user_profiles @relation(fields: [rated_by], references: [id], onUpdate: NoAction)
  vendors       vendors       @relation(fields: [vendor_id], references: [id], onUpdate: NoAction)

  @@index([vendor_id], map: "idx_vendor_performance_ratings_vendor")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model visitors {
  id                                                     String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  visitor_code                                           String         @unique @db.VarChar(30)
  full_name                                              String         @db.VarChar(150)
  email                                                  String         @db.VarChar(150)
  phone                                                  String         @db.VarChar(20)
  company                                                String         @default("") @db.VarChar(150)
  government_id                                          String         @default("") @db.VarChar(50)
  host_employee_id                                       String         @db.Uuid
  location_id                                            String         @db.Uuid
  status                                                 visitor_status @default(REGISTERED)
  check_in_time                                          DateTime?      @db.Timestamptz(6)
  check_out_time                                         DateTime?      @db.Timestamptz(6)
  badge_number                                           String         @default("") @db.VarChar(30)
  notes                                                  String         @default("")
  created_by                                             String         @default(dbgenerated("auth.uid()")) @db.Uuid
  created_at                                             DateTime       @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                                             DateTime       @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  appointments                                           appointments[]
  user_profiles_visitors_created_byTouser_profiles       user_profiles  @relation("visitors_created_byTouser_profiles", fields: [created_by], references: [id], onUpdate: NoAction)
  user_profiles_visitors_host_employee_idTouser_profiles user_profiles  @relation("visitors_host_employee_idTouser_profiles", fields: [host_employee_id], references: [id], onUpdate: NoAction)
  locations                                              locations      @relation(fields: [location_id], references: [id], onUpdate: NoAction)

  @@index([email], map: "idx_visitors_email")
  @@index([host_employee_id], map: "idx_visitors_host_employee")
  @@index([location_id], map: "idx_visitors_location")
  @@index([status], map: "idx_visitors_status")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workflow_execution_history {
  id                    String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workflow_execution_id String              @db.Uuid
  step_order            Int?
  step_name             String?             @db.VarChar(150)
  status                String?             @db.VarChar(30)
  executed_at           DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  result                Json?
  error                 String?
  workflow_executions   workflow_executions @relation(fields: [workflow_execution_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([workflow_execution_id], map: "idx_workflow_execution_history_execution")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workflow_executions {
  id                         String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workflow_id                String                       @db.Uuid
  trigger_event              String                       @db.VarChar(50)
  entity_type                String                       @db.VarChar(50)
  entity_id                  String                       @db.Uuid
  status                     workflow_execution_status    @default(RUNNING)
  current_step_index         Int                          @default(0)
  started_at                 DateTime                     @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  completed_at               DateTime?                    @db.Timestamptz(6)
  error                      String?
  created_at                 DateTime                     @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at                 DateTime                     @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  workflow_execution_history workflow_execution_history[]
  workflows                  workflows                    @relation(fields: [workflow_id], references: [id], onUpdate: NoAction)
  workflow_logs              workflow_logs[]

  @@index([entity_type, entity_id], map: "idx_workflow_executions_entity")
  @@index([status], map: "idx_workflow_executions_status")
  @@index([workflow_id], map: "idx_workflow_executions_workflow")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workflow_logs {
  id                  String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workflow_id         String              @db.Uuid
  execution_id        String              @db.Uuid
  step_name           String              @db.VarChar(150)
  action_type         String              @db.VarChar(50)
  status              workflow_log_status
  occurred_at         DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  details             Json?
  created_at          DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at          DateTime            @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  workflow_executions workflow_executions @relation(fields: [execution_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  workflows           workflows           @relation(fields: [workflow_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([execution_id], map: "idx_workflow_logs_execution")
  @@index([workflow_id], map: "idx_workflow_logs_workflow")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workflow_steps {
  id          String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workflow_id String             @db.Uuid
  step_order  Int
  name        String             @db.VarChar(150)
  type        workflow_step_type
  config      Json
  workflows   workflows          @relation(fields: [workflow_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([workflow_id, step_order], map: "uq_workflow_steps_order")
  @@index([workflow_id], map: "idx_workflow_steps_workflow")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workflows {
  id                  String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                String                 @db.VarChar(150)
  code                String                 @unique @db.VarChar(50)
  description         String?                @default("")
  trigger_event       workflow_trigger_event
  status              workflow_config_status @default(ACTIVE)
  version             Int                    @default(1)
  created_by          String                 @default(dbgenerated("auth.uid()")) @db.Uuid
  created_at          DateTime               @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  updated_at          DateTime               @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  workflow_executions workflow_executions[]
  workflow_logs       workflow_logs[]
  workflow_steps      workflow_steps[]
  user_profiles       user_profiles          @relation(fields: [created_by], references: [id], onUpdate: NoAction)

  @@index([status], map: "idx_workflows_status")
  @@index([trigger_event], map: "idx_workflows_trigger_event")
  @@schema("public")
}

enum aal_level {
  aal1
  aal2
  aal3

  @@schema("auth")
}

enum code_challenge_method {
  s256
  plain

  @@schema("auth")
}

enum factor_status {
  unverified
  verified

  @@schema("auth")
}

enum factor_type {
  totp
  webauthn
  phone
  recovery_code

  @@schema("auth")
}

enum oauth_authorization_status {
  pending
  approved
  denied
  expired

  @@schema("auth")
}

enum oauth_client_type {
  public
  confidential

  @@schema("auth")
}

enum oauth_registration_type {
  dynamic
  manual

  @@schema("auth")
}

enum oauth_response_type {
  code

  @@schema("auth")
}

enum one_time_token_type {
  confirmation_token
  reauthentication_token
  recovery_token
  email_change_token_new
  email_change_token_current
  phone_change_token

  @@schema("auth")
}

enum audit_action {
  INSERT
  UPDATE
  DELETE
  STATUS_CHANGE
  APPROVE
  REJECT
  CONVERT
  ISSUE
  RECEIVE
  CLOSE
  CANCEL

  @@schema("public")
}

enum batch_status {
  SCHEDULED
  IN_PROGRESS
  COMPLETED
  CANCELLED

  @@schema("public")
}

enum inventory_txn_type {
  INWARD_PURCHASE
  OUTWARD_DISPOSAL
  STOCK_ADJUSTMENT
  MFG_CONSUMPTION
  MFG_PRODUCTION
  DYNAMIC_RECONCILIATION

  @@schema("public")
}

enum item_classification {
  RAW_MATERIAL
  PACKAGING
  EMULSIFIER
  CONSUMABLE
  FINISHED_GOOD

  @@schema("public")
}

enum master_data_status {
  DRAFT
  ACTIVE
  INACTIVE

  @@schema("public")
}

enum plan_status {
  DRAFT
  ACTIVE
  COMPLETED
  CANCELLED

  @@schema("public")
}

enum po_status {
  DRAFT
  ISSUED
  PARTIALLY_RECEIVED
  RECEIVED
  CLOSED
  CANCELLED

  @@schema("public")
}

enum pr_status {
  DRAFT
  SUBMITTED
  APPROVED
  REJECTED
  CONVERTED
  CANCELLED

  @@schema("public")
}

enum price_source {
  QUOTE
  CONTRACT
  SPOT
  PO_HISTORY

  @@schema("public")
}

enum user_role {
  admin
  editor
  viewer

  @@schema("public")
}

enum vendor_status {
  DRAFT
  APPROVED
  ACTIVE
  SUSPENDED
  BLACKLISTED

  @@schema("public")
}

enum appointment_status {
  DRAFT
  REQUESTED
  APPROVED
  SCHEDULED
  EXPECTED
  CHECKED_IN
  IN_VISIT
  CHECKED_OUT
  RESCHEDULED
  REJECTED
  NO_SHOW
  CANCELLED

  @@schema("public")
}

enum approval_decision_action {
  APPROVE
  REJECT

  @@schema("public")
}

enum approval_entity_type {
  PURCHASE_ORDER
  PURCHASE_REQUEST
  PRODUCTION_ORDER
  VENDOR_MASTER
  INVENTORY_ADJUSTMENT
  BOM
  PRODUCTION_PLAN

  @@schema("public")
}

enum approval_request_status {
  PENDING
  APPROVED
  REJECTED
  ESCALATED
  CANCELLED

  @@schema("public")
}

enum contract_status {
  ACTIVE
  EXPIRED
  PENDING

  @@schema("public")
}

enum email_log_status {
  QUEUED
  PROCESSING
  SENT
  DELIVERED
  FAILED
  RETRYING
  BOUNCED

  @@schema("public")
}

enum email_queue_status {
  QUEUED
  PROCESSING
  SENT
  FAILED
  RETRYING

  @@schema("public")
}

enum email_template_category {
  VISITOR
  APPOINTMENT
  WORKFLOW
  SYSTEM
  NOTIFICATION

  @@schema("public")
}

enum mrp_run_status {
  IN_PROGRESS
  COMPLETED
  FAILED
  CONVERTED

  @@schema("public")
}

enum notification_recipient_role {
  ADMIN
  APPROVER
  ALL

  @@schema("public")
}

enum notification_severity {
  INFO
  WARNING
  SUCCESS
  ERROR

  @@schema("public")
}

enum notification_type {
  NEW_REGISTRATION
  ACCOUNT_APPROVED
  ACCOUNT_REJECTED
  ROLE_CHANGED
  SCOPE_CHANGED
  PENDING_APPROVAL
  ACCESS_REMOVED
  ACCESS_TRANSFERRED
  SYSTEM_ALERT

  @@schema("public")
}

enum planning_requirement_action {
  SUFFICIENT
  PROCURE
  PRODUCE
  PARTIAL_STOCK

  @@schema("public")
}

enum planning_requirement_shortage_reason {
  SUFFICIENT
  INSUFFICIENT_STOCK
  LATE_SUPPLY
  SAFETY_STOCK_REPLENISHMENT
  MOQ_EFFECT
  LOT_SIZE_ROUNDING
  MISSING_BOM
  CIRCULAR_BOM

  @@schema("public")
}

enum planning_requirement_status {
  PENDING
  CONVERTED_TO_PLAN
  CONVERTED_TO_PO
  DISMISSED

  @@schema("public")
}

enum production_order_status {
  DRAFT
  PENDING_APPROVAL
  APPROVED
  MATERIAL_ALLOCATED
  IN_PROGRESS
  QUALITY_CHECK
  COMPLETED
  REJECTED
  CLOSED
  CANCELLED

  @@schema("public")
}

enum quality_checkpoint {
  INCOMING
  IN_PROCESS
  FINISHED_GOODS

  @@schema("public")
}

enum quality_disposition {
  PENDING
  PASS
  FAIL
  REWORK
  SCRAP
  RELEASE

  @@schema("public")
}

enum visitor_status {
  REGISTERED
  REQUESTED
  APPROVED
  SCHEDULED
  EXPECTED
  CHECKED_IN
  IN_VISIT
  CHECKED_OUT
  REJECTED
  NO_SHOW
  CANCELLED

  @@schema("public")
}

enum workflow_config_status {
  ACTIVE
  INACTIVE
  DRAFT

  @@schema("public")
}

enum workflow_execution_status {
  RUNNING
  COMPLETED
  FAILED
  PAUSED
  CANCELLED

  @@schema("public")
}

enum workflow_log_status {
  SUCCESS
  FAILED
  SKIPPED

  @@schema("public")
}

enum workflow_step_type {
  CONDITION
  APPROVAL
  EMAIL
  DATABASE_UPDATE
  DELAY
  NOTIFICATION

  @@schema("public")
}

enum workflow_trigger_event {
  visitor_created       @map("visitor.created")
  visitor_updated       @map("visitor.updated")
  visitor_approved      @map("visitor.approved")
  visitor_rejected      @map("visitor.rejected")
  appointment_created   @map("appointment.created")
  appointment_approved  @map("appointment.approved")
  appointment_rejected  @map("appointment.rejected")
  appointment_cancelled @map("appointment.cancelled")
  visitor_checked_in    @map("visitor.checked_in")
  visitor_checked_out   @map("visitor.checked_out")
  inventory_adjusted    @map("inventory.adjusted")
  production_completed  @map("production.completed")

  @@schema("public")
}
